export interface info {
    icon: React.JSX.Element;
    title: string;
    text: string;
}

export interface menutype {
    title: string;
    // component: React.JSX.Element;
    // active: boolean;
    href: string;
}
